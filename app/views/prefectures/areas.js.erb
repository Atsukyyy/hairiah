// $('#area_id').html("<%= escape_javascript(select_tag options_for_select(@areas.pluck(:name, :id))) %>");
$('#area_id').html("<%= escape_javascript(select_tag :area_id, options_for_select(@areas.pluck(:name, :id)), include_blank: true) %>");
$('#user_area_id').html("<%= escape_javascript(options_for_select(@areas.pluck(:name, :id))) %>");
